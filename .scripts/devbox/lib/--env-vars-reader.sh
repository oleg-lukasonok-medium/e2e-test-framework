#!/bin/bash
# -------------------------------------------------------------------------------------
#
#
#
# -------------------------------------------------------------------------------------

. ./.scripts/base/lib/--env-vars-reader.sh

ALL_ARGS=("$@")
while [[ "$#" -gt 0 ]]; do
  case $1 in
  *) ;;
  esac
  shift
done
set -- "${ALL_ARGS[@]}"
